# Not separate Flow custom functions

Flow has **no File parameter type**. These scripts take `merged_file` and cannot be pasted as standalone Flow CFs.

Their logic is already inlined into `../publish_quote_package.deluge`. Keep this folder as source reference only.
