<!-- Generated by zoho-task-repo-lifecycle -->
<!-- Task: BI1-T71 -->
<!-- Repository lifecycle version: 2 -->

# Claude Instructions

Zoho Task ID: 2543412000001469015

- Scope all work to `BI1-T71` and this repository.
- Read `README.md` and `TASK.md` before changing files.
- Read `docs/PROCESS.md` at the start of each session and follow the documented handoff loop.
- Never expose secrets, tokens, `.env` contents, or private customer data.
- Do not push directly to the default branch without explicit approval.
- Keep implementation in `scripts/`, documentation in `docs/`, and approved supporting files in `artifacts/`.
- Every Claude/Codex session must update `docs/CURRENT_HANDOFF.md` before stopping work. Treat it as the temporary active-session handoff, not permanent project documentation.

Template reuse from `zoho_task_folder_sync.py` is deferred for this MVP to avoid modifying or importing the active automation.
